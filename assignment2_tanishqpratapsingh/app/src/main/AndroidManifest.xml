<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />




    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"


        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Assignment2_tanishqpratapsingh"
        tools:targetApi="31">

        <!-- Details screen (second screen) -->
        <activity
            android:name=".DetailsCityActivity"
            android:exported="false" />

        <!-- Main screen (first screen + launcher) -->
        <activity
            android:name=".LocationCityActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- REMOVE MainActivity completely -->
        <!-- If you haven't deleted MainActivity.java yet, delete it from the project -->

    </application>

</manifest>
